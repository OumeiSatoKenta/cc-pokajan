# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# 環境変数・シークレット
# Vite テンプレートの `*.local` は .env.local しか拾わないため、.env / .env.production を明示する。
# 雛形として共有したい .env.example だけは除外を打ち消す。
.env
.env.*
!.env.example

# Playwright
test-results
playwright-report
blob-report
.playwright

# Claude Code のローカル設定（各自の環境依存。共有しない）
.claude/settings.local.json
