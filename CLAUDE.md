# cc-pokajan プロジェクトメモ

「ホロライブドリームス」のカジノミニゲーム **「ポカジャン！」** を再現したブラウザゲーム。
1人 + CPU3人での対局と、BET → 対局 → 精算のカジノループをブラウザ上で完結させる。

バックエンド・データベース・クラウドを持たない**完全にクライアントサイドのみ**のプロジェクト。

## 技術スタック

- TypeScript ~6.0（`strict` / `verbatimModuleSyntax` / `erasableSyntaxOnly`）
- Vite 8 + React 19
- Vitest 4（テスト環境は `node`。DOM が要る箇所は `renderToStaticMarkup` で代替し jsdom を入れていない）
- oxlint（Vite テンプレートの既定。ESLint は使っていない）
- Prettier
- パッケージマネージャー: npm

永続化は localStorage（所持コイン・ルール差分・座席アバターの対応）と
IndexedDB（画像 Blob）。サーバーは存在しない。

## このプロジェクト固有のルール

実装前に [README.md](README.md) の「設計方針」節と、直近の `.steering/*/design.md` を読むこと。
特に以下はテストとlintで機械的に守られており、破ると CI 相当のゲートで落ちる。

- **エンジン層（`src/engine/`）は React と `src/config/` に依存しない**。
  `.oxlintrc.json` の `no-restricted-imports` で禁止しており `npm run lint` が検知する
- **エンジンは `Math.random()` と `Date` を使わない**。乱数は `Rng` を引数で受け取る。
  これにより対局の全経過が「初期シード + アクション列」だけで再現できる
- **可変数値は `RulesConfig` に集約**し、全関数が `rules` を引数で受け取る。
  型は `src/engine/types.ts`、値は `src/config/rules.ts`
- **CPU AI は `GameState` を受け取らない**。公開情報だけの `AiView` を受け取るため、
  他家の手札に到達する経路が型として存在しない
- **正しさを「たまたま成り立っている条件」に依存させない**。
  Step 2・3 で見つかった欠陥はすべてこの形をしていた
- **不正な入力を黙って無視しない**。`switch` を書いたら必ず `default` を置き、
  判別共用体には `never` への代入による網羅性検査を付ける
  （`switch` の後に `return` があると TypeScript の網羅性検査は働かない）

## 検証ゲート

作業の完了前に以下がすべて通ること。

```bash
npm run lint && npm run typecheck && npm test && npm run build && npm run format:check
```

対局ロジックの不変条件（点数保存則・カード保存則・手札枚数）は
`tests/engine/autoplay.test.ts` が100局の全ステップで検査している。
エンジンに手を入れたらここが最初の砦になる。

## 外部ライブラリ・API調査のルール

外部ライブラリやAPIの使い方・仕様について質問された場合、または実装中にライブラリの
ドキュメントを参照する必要がある場合は、Context7 MCP（`resolve-library-id` → `query-docs`）を
自動的に使用して最新ドキュメントを取得すること。ユーザーが「use context7」と明示しなくても適用する。

Context7 は `cc-base-tools` プラグインが提供するため、`.mcp.json` には定義していない（重複回避）。

## MCP・スキルの自動使用ルール

以下のルールに従い、関連するMCPサーバーやスキルを**明示的な指示がなくても自動的に使用する**こと。
一般知識での回答よりMCP/スキル経由の公式ドキュメントを優先する。

### 図の作成

draw.io 形式の図（アーキテクチャ図・状態遷移図など）が必要なときは `drawio` MCP を使用する。
Mermaid からの変換にも対応している。

> **補足**: AWS / Notion / TiDB の各 MCP はこのプロジェクトでは使わないため `.mcp.json` から
> 外してある。将来必要になったら `/scaffold-project` のテンプレートから復活させること。

## スペック駆動開発の基本原則

### 基本フロー

1. **ドキュメント作成**: 永続ドキュメント(`docs/`)で「何を作るか」を定義
2. **作業計画**: ステアリングファイル(`.steering/`)で「今回何をするか」を計画
3. **実装**: tasklist.mdに従って実装し、進捗を随時更新
4. **検証**: テストと動作確認（完了前の検証原則を適用）
5. **更新**: 必要に応じてドキュメント更新

### 重要なルール

#### ドキュメント作成時

**1ファイルずつ作成し、必ずユーザーの承認を得てから次に進む**

承認待ちの際は、明確に伝える:

```
「[ドキュメント名]の作成が完了しました。内容を確認してください。
承認いただけたら次のドキュメントに進みます。」
```

#### 実装前の確認

新しい実装を始める前に、必ず以下を確認:

1. CLAUDE.mdを読む
2. 関連する永続ドキュメント(`docs/`)と直近の `.steering/*/design.md` を読む
3. Grepで既存の類似実装を検索
4. 既存パターンを理解してから実装開始

#### ステアリングファイル管理

作業ごとに `.steering/[YYYYMMDD]-[タスク名]/` を作成:

- `requirements.md`: 今回の要求内容
- `design.md`: 実装アプローチ
- `tasklist.md`: 具体的なタスクリスト（進捗追跡・レビューセクションを含む）

命名規則: `20250115-add-user-profile` 形式

**作業計画・実装・検証時は`steering`スキルを使用してください。**

- **作業計画時**: `Skill('steering')`でモード1(ステアリングファイル作成)
- **実装時**: `Skill('steering')`でモード2(実装とtasklist.md更新管理)
- **検証時**: `Skill('steering')`でモード3(振り返り)

## ワークフロー・オーケストレーション

### 1. プランモードの原則適用

- 3ステップ以上の作業や、アーキテクチャ上の決定を伴う重要なタスクでは、必ずプランモード
  （steeringスキル モード1によるステアリングファイル作成）に入ること。
- 作業が停滞したり予期せぬ方向へ進んだりした場合は、即座に中断して計画を練り直すこと。
- 曖昧さを排除するため、事前に詳細な仕様を記述すること。

**実装前のドキュメントレビューを省略しないこと。** Step 3 では着手前のレビューが
不変条件の定義式の誤りを検出した。実装後に気づいた場合、正しい実装を「壊れている」と
誤検知するか、誤った式に合わせて本物のバグを作り込むかのどちらかになっていた。

### 2. サブエージェントの活用戦略

- メインのコンテキストウィンドウをクリーンに保つため、サブエージェントを積極的に活用すること。
- 調査、探索、並列分析などはサブエージェントに任せること。
- 集中して実行できるよう、1つのサブエージェントにつき1つのタスクを割り当てること。
- **サブエージェントの出力が壊れていたら受け入れず、指示を明確にして再実行すること。**
  Step 2 では壊れた出力を再実行した結果、実在の欠陥が3件見つかった。

### 2.5. Agent Teamsの活用戦略

| シナリオ                     | Teams    | サブエージェント |
| ---------------------------- | -------- | ---------------- |
| 3ファイル以上の並行変更      | **推奨** | -                |
| 実装 + テスト作成の同時進行  | **推奨** | -                |
| 複数観点からのコードレビュー | **推奨** | -                |
| 調査・情報収集のみ           | -        | **推奨**         |
| 単一ファイルの変更           | -        | **推奨**         |

**運用ルール**:

- **ファイル競合の回避**: 各チームメイトが担当するファイルを明確に分離する
- **タスク粒度**: チームメイト1人あたり5-6タスクを目安にする
- **コンテキスト共有**: チームメイトにはスペックファイルのパスと担当範囲を明示する
- **完了基準**: 全チームメイトの作業が完了し、lint・テストが通ることを確認してから完了とする

### 3. 自己改善ループ

- ユーザーから修正指示を受けた後は、そのパターンを該当する `knowledge/` ファイルに記録すること。
- 同じミスを繰り返さないよう、自分自身に対するルールを策定すること。
- セッション開始時には、当該プロジェクトに関連する教訓を必ず復習すること。

### 4. 完了前の検証

- 動作の証明ができるまで、タスクを完了と見なさないこと。
- **「テストが全部通っている」ことは「正しい」ことを意味しない。**
  Step 2 の欠陥3件はいずれも186件のテストを通過した状態で潜んでいた。
  構造的整合性のテストだけでは偽陰性（本来成立すべきものの取りこぼし）を検出できないため、
  重要なロジックには素朴な別実装との突き合わせを用意すること。
- テストを実行し、ログを確認し、正当性を実証すること。
- 「スタッフエンジニアならこれを承認するか？」と自問自答すること。

### 5. エレガントさの追求（バランス重視）

- 重要な変更を加える際は、一度立ち止まって「よりエレガントな方法はないか？」を考えること。
- 単純で明白な修正については、過剰なエンジニアリングを避けるためにこのステップを省略すること。

### 6. 自律的なバグ修正

- バグ報告を受けた際は、手取り足取りの指示を待たずに自力で修正すること。
- ログやエラー、失敗したテストを特定し、それらを解決すること。

## タスク管理

ステアリングファイル(`.steering/[YYYYMMDD]-[タスク名]/tasklist.md`)を中心に進捗を管理する:

1. **計画優先**: tasklist.mdにチェックリスト形式で計画を書き出す。
2. **計画の確認**: 実装を開始する前に、方針に間違いがないか確認する。
3. **進捗の追跡**: 完了した項目は随時チェックを入れる。
4. **変更の説明**: 各ステップで、何を行ったかハイレベルな要約を提示する。
5. **結果の記録**: tasklist.mdに振り返りセクションを追加する。

**フェーズの区切りごとにファイルサイズを機械的に測ること**（`wc -l`）。
Step 3 では「400行を超えたら分割する」と設計書に書いておきながら641行まで放置し、
3つのレビューすべてから同じ指摘を受けた。基準は文書に書くだけでは機能しない。

## ディレクトリ構造

```
src/
  engine/          ルールエンジン（純粋TS・React非依存）
  config/          ルール値と同梱ロスター
  ui/              画面・フック・部品（board.css は卓枠/席/河、center.css は卓中央、
                   table.css は手番まわり、win.css は和了演出、hints.css は残り枚数、
                   landscape.css は横向き）
  storage/         localStorage（prefs）/ IndexedDB（assets）
tests/             Vitest（engine / config / storage / ui）と Playwright（e2e）
docs/ideas/        計画書（pokajan-plan.md / pokajan-mahjong-board-plan.md ほか）
docs/design/       デザイン依頼書（brief.md）と現状のスクリーンショット（shots/）
.steering/         作業単位のドキュメント
```

### 永続的ドキュメント(`docs/`)

現状は `docs/ideas/` の計画書のみ。正式版ドキュメント
（product-requirements.md / functional-design.md / architecture.md /
repository-structure.md / development-guidelines.md / glossary.md）は未作成で、
必要になった時点で `/setup-project` で作成する。

実質的な設計の記録は `.steering/*/design.md` と README.md にある。

## 実装状況

当初計画（Step 1〜6）は**全て完了**。計画は
[docs/ideas/pokajan-add-feature-commands.md](docs/ideas/pokajan-add-feature-commands.md) を参照。

**盤面を麻雀ゲームに近づける Step 7 も完了**。計画は
[docs/ideas/pokajan-mahjong-board-plan.md](docs/ideas/pokajan-mahjong-board-plan.md) と
[docs/ideas/pokajan-mahjong-board-add-feature-commands.md](docs/ideas/pokajan-mahjong-board-add-feature-commands.md)。

**プレイテスト反映の Step 8 も完了**。計画は
[docs/ideas/pokajan-presentation-and-counts-plan.md](docs/ideas/pokajan-presentation-and-counts-plan.md)。

**外注デザイン第2稿をカジノ風に反映する Step 9 も完了**。計画は
[docs/ideas/pokajan-casino-table-redesign-plan.md](docs/ideas/pokajan-casino-table-redesign-plan.md) と
[docs/ideas/pokajan-casino-table-redesign-add-feature-commands.md](docs/ideas/pokajan-casino-table-redesign-add-feature-commands.md)。

**プレイテスト反映の Step 10 も完了**。計画は
[docs/ideas/pokajan-playtest-followup-plan.md](docs/ideas/pokajan-playtest-followup-plan.md) と
[docs/ideas/pokajan-playtest-followup-add-feature-commands.md](docs/ideas/pokajan-playtest-followup-add-feature-commands.md)。
**10-3 で 9-3 保留の「横向きの縦 fit（844×390）」を解消した**。

| Step | 内容                              | 状態                             |
| ---- | --------------------------------- | -------------------------------- |
| 1    | 基盤・型定義・山札生成            | ✅ 完了                          |
| 2    | 役判定・点数・支払い              | ✅ 完了                          |
| 3    | 対局状態機械 + CPU AI             | ✅ 完了                          |
| 4    | 対局UI                            | ✅ 完了                          |
| 4b   | プレイテスト反映（後から追加）    | ✅ 完了                          |
| 5    | カジノメタ                        | ✅ 完了                          |
| 6    | ロスターエディタ + ルール設定     | ✅ 完了                          |
| 6b   | カードの見た目（後から追加）      | ✅ 完了                          |
| 7-1  | カードの表現（裏面・河・タイル）  | ✅ 完了                          |
| 7-2  | 4方向レイアウト                   | ✅ 完了                          |
| 7-3  | プレイヤーアバター                | ✅ 完了                          |
| 7-4  | 和了の確認ゲート                  | ✅ 完了                          |
| 7-5  | 和了演出の中身                    | ✅ 完了                          |
| 8-1  | 和了演出の作り込み（2段・自動閉） | ✅ 完了                          |
| 8-2  | 残り枚数の確認                    | ✅ 完了                          |
| 9-1  | カジノ風テーマの全面刷新          | ✅ 完了                          |
| 9-2  | 河の直近5枚固定 + ヘッダー        | ✅ 完了                          |
| 9-3  | 横向きレイアウト + 待ちチップ     | ✅ 完了（縦 fit は 10-3 で解消） |
| 10-1 | 待ちのホバー/タップ展開           | ✅ 完了                          |
| 10-2 | ゲーム風ボタン（全画面）          | ✅ 完了                          |
| 10-3 | 横向き専用レイアウト再設計        | ✅ 完了（844×390 の縦 fit 達成） |

Step 4b と 6b は当初計画になく、実プレイで見つかった問題への対応として追加した。

- **4b**: グループ構成が見えない・持ち時間が短い・手札が整列されない
- **6b**: アップロード画像が切り取られる・カードからグループが分からない

**自動テストが全部通っていてもプレイテストでしか見つからない欠陥がある**ことの記録。
6b では、Step 6 で書いた `.card__image` の CSS が丸ごと欠落していたことも判明した
（画像を入れた状態で対局画面を開いていなかった）。

Step 7-2 の完了時点で対局画面は**上家=左 / 対面=上 / 下家=右 / 自分=下の3×3グリッド**になり、
4人全員が伏せ手札と河を持つ。席の向きは `seatOrientation`（`src/ui/labels.ts`）が
`humanSeat` からの相対位置で決めるため、人間がどの席でも卓は回らない。
CSS は卓枠/席/河（`src/ui/board.css`）・卓中央（`src/ui/center.css`）・手番まわり（`src/ui/table.css`）・
和了演出（`src/ui/win.css`）・残り枚数の手がかり（`src/ui/hints.css`）・横向き（`src/ui/landscape.css`）に
分かれている（Step 9 で center / landscape を追加）。`.overlay*`（全画面の覆いとパネルの枠）は
`ResultOverlay` と `ErrorBoundary` も使うため `table.css` に残してある。
**横向きの上書きは import 順で後勝ちさせるため `landscape.css` を最後に読み込む**。
App.css の同名ルールと同詳細度で競合する箇所（伏せ札・河の小カード）は `.seat`/`.river` を挟んで勝たせる。

Step 7-3 で**座席ごとのアバター**が入った。画像は既存の IndexedDB
（`src/storage/assets.ts`）にロスターの画像と同居し、座席→画像IDの対応だけを
`prefs` に持つ。座席は**絶対番号**（`PlayerId`）で持ち、席名は表示のときだけ相対に直す。

> **画像を参照するものを増やすときは `usedImageIds`（`src/ui/rosterEditor.ts`）に
> 引数を足すこと。** この関数の戻り値は `pruneImages`（渡した ID 以外を削除）と
> `nextId`（採番）と書き出しの3つに使われる。数え落とすと**画像が黙って消える**。
> 必須引数にしてあるので、足せば全呼び出しが型エラーになる。

Step 7-4 で**和了が対局を止める**ようになった。停止は2層で、
`useGameLoop` の3つの効果と、リデューサの `ENGINE` / `TIMEOUT` の弾き。
**効果を止めるだけでは人間のクリックが止まらない**ため両方要る。
演出待ちの間も `EVENTS_CONSUMED` は意図的に通す（表示済みイベントの掃除は進行ではない）。

> **E2E の進行手順は `tests/e2e/helpers/table.ts` の1本に集約してある。**
> 以前は `playToEnd` が `table.spec.ts` と `casino.spec.ts` に写しであり、
> 7-4 で同じ修正を2回書く羽目になった。進行の手順を変えるときはここだけを直す。

Step 7-5 で**カットイン・得点の増減・順位の移動**が入り、Step 7 は完了した。
順位は `computeRanking`（`src/engine/gameSelectors.ts`）を**終局時と演出で共有する**。
順位はそのまま順位倍率＝精算額になるため、画面側に並べ替えを書くと
「演出の順位と精算の順位が違う」という最も分かりにくい不一致が起こる。

> **Playwright の `test.use({ reducedMotion: 'reduce' })` はこの構成では
> ページに届かない**（`matchMedia` が false のまま）。`page.emulateMedia()` を使い、
> 設定が届いたこと自体を DOM 属性で確かめること。
> 7-5 ではこれに気づかず、一度**偽陽性のテスト**を書いた。

Step 8-1 で演出が**カットイン → 点数獲得結果の2段**になり、確認ボタンが消えて
**数秒で自動的に閉じる**ようになった。段の状態機械は `WinOverlay` が持つ
（`useGameLoop` に置くと「停止フラグを持つ効果」が4つに増える）。
同色役は大物手バージョンで、判定は `isBigWin`（`src/config/presentation.ts`）の1箇所。

> **閉じる操作は鍵で照合する。** `DISMISS_WIN` は `winKey`（`loopReducer.ts`）を伴い、
> 先頭の和了と一致しなければ何もしない。オーバーレイ全体がクリックで進むため
> パネル内のボタンの click は必ず泡立ち、素直に書くと**2件落ちる**。
> `stopPropagation` は「今の DOM 構造ではたまたま漏れない」形の正しさになるので使わない。
> 同じ鍵が `WinOverlay` の React の `key` にもそのまま使える。

Step 8-2 で**残り枚数**が見えるようになり、Step 8 は完了した。
算出は `src/engine/unseen.ts`（`toVisibleCards` → `countUnseen` → `unseenOf`）。
表示は手札ホバーの `CardCounts` とテンパイ時の `WaitPanel`（CSS は `hints.css`）。

Step 10-1 で **`WaitPanel` は「待ち N件」トリガ＋フロー外オーバーレイ**になった
（`.table__mine-head` に常駐。ホバー覗き見＋クリックでピン留め、Escape/外側クリックで解除）。
手札の上の常時パネルをやめたのは、テンパイの成立/崩れで手札が上下していたため。
並び・6件上限・残0淡色の算出は不変で、器だけを差し替えている。

> **オーバーレイに幅を持たせるとき `min-width: max-content` を置かない。** `min-width` は
> `max-width` に勝つうえ、`flex-wrap` の中身では `max-content` が折り返し前提を無視して
> 「全部1行」の幅を返す。10-1 では待ち6件で**横1500px**に膨れ、同じ seed を使う E2E が
> **`scrollWidth` を見ていなかったため見逃した**。上限は `max-width` だけで与え、
> レスポンシブ検査は縦（`scrollHeight`）だけでなく**横（`scrollWidth`）も実測する**。

> **局をまたいで使い回す UI の一時状態は、表示条件が偽になる瞬間にリセットする。**
> `WaitPanel` は `key` を切らず同一インスタンスで使い回すため、テンパイ崩れ（`waits.length === 0`）で
> `null` を返しても**アンマウントされず `pinned` が生き残り**、再テンパイで無操作のまま開いて復活した。
> `useEffect([waits.length])` で解除する。「常にマウントされ続ける」に依存した正しさは崩れる。

Step 10-2 で**操作ボタンを色分け**した（ツモ=緑 `button--tsumo` / ロン=赤 `button--ron` / 見送る=
`button--ghost`。`ActionBar` が `item.kind` で出し分ける）。`.button` ベース（App.css）は全画面共通で
グラデ・影・押下感を持ち、`.button--primary`（金）も含めタイトル/BET/精算/設定に波及する。

> **別ファイルの `.button--x` 変種は、単一クラスだと `.button` ベースに import 順で負ける。**
> `App.tsx` は `import './App.css'` を**最後**に置くため、App.css の `.button`（0,1,0）が
> table.css の `.button--tsumo`（0,1,0）より後勝ちし、背景・文字色・太字を丸ごと潰す
> （影だけ残る幽霊ボタン）。**複合クラス `.button.button--tsumo`（0,2,0）** で詳細度を確保する
> （hover は 0,3,0）。9-1（`.card--clickable.card--waiting`）・9-3（`.seat .card--back`）と同じ
> 「同詳細度は import 順で負ける」罠の3度目。**色・詳細度勝敗は class 名テストでは見えない**ので、
> 実アプリでボタンを注入し `getComputedStyle().backgroundImage` に `gradient` が乗ることを E2E で実測する
> （`tests/e2e/table.spec.ts`）。`.button--primary`/`.button--ghost` は App.css 内で `.button` の後にあり
> 後勝ちで勝つため複合化は不要。

Step 10-3 で**横向き専用レイアウト**を DOM 再構成で実現し、**844×390 の縦 fit を達成**した
（9-3 の保留を解除）。`TableScreen` は手札（`.table__mine`）と操作バー（`.actions`）を新ラッパ
`.table__controls`（`.table__board` の `bottom` エリア）で包み、横向きで [手札 | 操作] のレールにする
（`landscape.css`）。操作バーは felt 内・手札の右へ移った（第2稿準拠）。縦・デスクトップは縦積みのまま。

> **横向きの縦 fit は「卓の外」も疑う。** 縦を最も食っていたのは卓ではなく、全画面共通の
> `app__header`（"ポカジャン" 見出し・約51px）だった。横向きの対局画面でだけ畳んで回収する。
> ただし **`display:none` にしない・スコープする**の2点が要（3軸レビューで両方捕まえた）:
> ① `landscape.css` はバンドル全体に効くので `.app[data-screen='table']`（`App.tsx` の `data-screen`）で
> 絞らないとタイトル/BET/精算/設定の見出しまで消える（タイトル画面は heading 0 件に）。
> ② `.table__title` は `<span>` で見出しではないため、`display:none` だと対局画面の heading が 0 件になる。
> **sr-only（画面外退避）で h1 を支援技術に残す**。E2E は「高さ0＋heading≥1」で両方を固定する。

> **横向きレールの操作バーは高さ上限＋スクロールにする。** 宣言候補は普通に3〜4個並ぶ
> （既定ルールで同一メンバーが複数色で揃う）。`align-items: stretch` だと背の高い操作バーが
> 手札ブロックまで引き伸ばし、レール行＝操作バー高で縦があふれる（seed により +19〜56px）。
> `align-items: flex-start` にして行高は手札が決め、`.actions { max-height; overflow-y: auto }` で
> バー内スクロールにする。ボタンは価値の高い順（`actionBarItems`）なので上位は先頭に見える。
> **E2E はボタンを注入して多ボタンの縦あふれを回帰ガードする**（初期状態〔0〜2ボタン〕だけ見ると見逃す）。

> **`min-height:100vh` の要素は子の縦マージンが突き抜ける（margin collapse）。** app__header を
> フローから外すと `.table` が `.app`（min-height:100vh・BFC でない）の先頭の in-flow 子になり、
> `.table` の上マージンが `.app` を貫いて 100vh の外へ出て約2pxあふれた。横向きで `.table { margin: 0 }`
> にして解消（根治は `.app { display: flow-root }` だが今回は局所対応）。

> **横向き fit は 844×390 で担保。より狭い横向き（iPhone SE 等）は据え置き（正直な限界）。**
> チューニングが 844px 幅前提のため、狭い幅では席見出しの折り返しでなお数十pxあふれる。
> 全端末幅の完全対応は将来の per-width チューニング（9-3 と同じ「到達値で正直に据え置く」姿勢）。

> **`countUnseen` は `GameState` を受け取らない。** 状態に触るのは `toVisibleCards` の
> 1箇所だけで、以降は公開情報だけの `VisibleCards` で完結する。`AiView` と同じ考え方で、
> **他家の手札へ到達する経路が型として存在しない**。`AiView` にフィールドを足して
> 流用しないこと（AI が読まない情報を増やすと 7-5 の `payments` と同じ負債になる）。

> **残枚数の引き当てに失敗したら 0 を返さず例外にする。** この画面で「残0」は
> 「その待ちはもう上がれない」という意味を持つため、数え落としを 0 で埋めると
> **この機能が防ごうとしている誤りをこの機能が生む**。
> 残枚数は上限であって確定値ではない（他家の手札と、`buildDeck` が
> プールから抜かなかった分を含む）ので、画面も「見えていない枚数」と書く。

> **ホバーの受け口は手札の `<li>` に置く。`CardView` には置かない。**
> 捨てられない局面の `CardView` は `disabled` の `<button>` になり、
> **無効化されたボタンにはマウスイベントが来ない**。カード側に付けると
> 「自分の捨てる番のときしか調べられない」機能になる。

**新しく書いた回帰テストは、わざと実装を壊して「落ちること」を確かめる。**
Step 7-3・7-4・7-5・8-1・8-2 でこの手順を回し、7-5・8-1・8-2 で
**何も見ていないテスト**を1件ずつ捕まえた。通ることしかできない検査は、
検証したつもりだけを残す。

> **同じボタンが2つの判断に使われているなら、ボタンで進行を観測しない。**
> 8-2 で既存の E2E（`人間が宣言窓で迷っている間に…`）が6回中3回落ちることが分かった。
> 「見送るボタンが消える」で受付の終了を見ていたが、このボタンは
> **割り込みの見送りと宣言の見送りの両方**に使われる。進行が正しく再開して
> 次の機会が来ればボタンは戻る。「割り込みタイマーが消える」も同じ理由で使えない
> （次の捨て札ですぐ新しい受付が開く）。**山札の残りだけが一方向に動く。**

> **停止・待機の検査は「壊したら何が動くか」を先に1つの値に決める。**
> 8-1 では持ち時間の停止を外しても E2E が通った。`decideTimeout` は
> `selfDeclare` で**宣言権者が人間のときだけ**時計を回すため、CPU の和了では
> 人間の時計がそもそも動いておらず、**最初から動かないものを検査していた**。
> 人間が和了する経路を足して初めて落ちるようになった。
> 7-4 が使っていた「タイマーの表示文字」も、`withTurnMs` が `minMs` を
> 一緒に下げるため短い持ち時間では変化せず、同じく素通りしていた。

## 著作権について

**デザインを外注するときは [docs/design/brief.md](docs/design/brief.md) を渡す。**
制約（特に下記の著作権）・技術条件・クラス名の目録・受け入れ基準をまとめてある。
「原作の見た目に寄せる」依頼にならないよう、依頼文の段階で防ぐこと。

ゲームルール自体に著作権は及ばないため再現に問題はないが、**公式イラスト・ロゴ・
キャラクター画像はリポジトリに一切含めない**。同梱のロスターはオリジナルの創作キャラで
構成しており、好みの素材はロスター設定画面からアップロードして差し替えられる。

## コア原則

- **シンプルさの追求**: あらゆる変更を可能な限りシンプルに保つこと。コードへの影響範囲を最小限に抑える。
- **妥協の排除**: 根本原因を突き止めること。一時しのぎの修正は行わない。
- **影響の最小化**: 必要な箇所のみを変更すること。不必要な変更によって新たなバグを作り込まない。
