{
  "name": "@pokajan/game-api",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "AWS Lambda backend (server-authoritative game-api). Shares src/engine via @engine/* aliases.",
  "scripts": {
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "test": "vitest run",
    "build": "node esbuild.config.mjs"
  },
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.700.0",
    "@aws-sdk/lib-dynamodb": "^3.700.0",
    "ulid": "^2.4.0"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.145",
    "@types/node": "^24.0.0",
    "esbuild": "^0.25.0"
  }
}
